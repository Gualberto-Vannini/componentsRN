export enum INPUT_FIELDS_TYPE {
  FIRST_NAME = 'FIRST NAME',
  LAST_NAME = 'LAST NAME',
  PASSWORD = 'PASSWORD',
  ABOUT_ME = 'ABOUT ME',
}

export enum MIN_CHAR {
  S = 3,
  M = 6,
}
export enum MAX_CHAR {
  S = 125,
  M = 150,
  L = 255,
}
